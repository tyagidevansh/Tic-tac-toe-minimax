import { ReactNode, FunctionComponent } from "react";
import { Manager as PopoverManager, Reference as PopoverReference } from "react-popper";
import { BSMultiElement, Triggers } from "../../types";
import { PopoverPopperProps } from "./PopoverPopper";
export interface PopoverProps extends Omit<PopoverPopperProps, "visible"> {
    content: ReactNode;
    children: (props: BSMultiElement) => JSX.Element;
    trigger?: Triggers | Triggers[];
    visible?: boolean;
    onToggle?: (visible: boolean) => void;
}
export declare const Popover: FunctionComponent<PopoverProps>;
export { PopoverManager, PopoverReference };
