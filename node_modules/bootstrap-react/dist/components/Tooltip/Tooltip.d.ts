import { FunctionComponent } from "react";
import { Manager as TooltipManager, Reference as TooltipReference } from "react-popper";
import { BSMultiElement, Triggers } from "../../types";
import { TooltipPopperProps } from "./TooltipPopper";
export interface TooltipProps extends Omit<TooltipPopperProps, "visible"> {
    content: JSX.Element | string;
    children: (props: BSMultiElement) => JSX.Element;
    trigger?: Triggers | Triggers[];
    visible?: boolean;
    onToggle?: (visible: boolean) => void;
}
export declare const Tooltip: FunctionComponent<TooltipProps>;
export { TooltipManager, TooltipReference };
