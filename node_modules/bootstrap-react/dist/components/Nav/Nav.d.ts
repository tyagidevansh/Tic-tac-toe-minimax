import React from "react";
import { BSList } from "../../types";
export interface NavProps extends BSList {
    variant?: "tabs" | "pills";
    layout?: "fill" | "justified";
}
export declare const Nav: React.ForwardRefExoticComponent<NavProps & React.RefAttributes<HTMLUListElement>>;
